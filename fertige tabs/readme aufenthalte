Vom Layout soweit fertig

Die buttons noch funktionslos (hat bei mir nicht funktioniert, müsste aber wie bei "stammdaten" gehen)

function für edatum == adatum für ambulant fehlt
function für Aufenthaltsdauer fehlt

